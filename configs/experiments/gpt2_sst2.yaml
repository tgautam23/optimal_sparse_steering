# GPT-2 Small + SST-2 Sentiment Steering
# Baseline experiment — nearly all defaults apply.
# Model: gpt2-small (768-dim, 12 layers)
# SAE:   gpt2-small-res-jb residual-stream JumpReLU SAEs
# Task:  Steer toward positive sentiment (target_class=1)

experiment_name: gpt2-small_sst2

model:
  steering_layer: 7          # best layer from probe sweep
